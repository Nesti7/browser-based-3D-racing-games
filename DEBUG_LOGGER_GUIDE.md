# 🔍 Инструкция по использованию DebugLogger

## Что это?

**DebugLogger** — это мощная отладочная панель, которая показывает в реальном времени:
- Координаты грузовика (физика и визуал)
- Скорость (м/с и км/ч)
- Углы поворота
- Состояние управления (газ, тормоз, повороты, ручник)
- Синхронизацию физики и визуала
- Состояние колёс
- Позицию камеры
- Последние нажатия клавиш

---

## 📦 Что добавлено

### Новые файлы:
- **js/DebugLogger.js** — Класс отладочной панели

### Изменённые файлы:
- **js/Game.js** — Создание и обновление DebugLogger
- **js/main.js** — Горячая клавиша L для логирования

---

## 🎮 Как использовать

### 1. Запусти игру
```
http://localhost:8000
```

### 2. Панель отладки
В **правом верхнем углу** появится зелёная панель с информацией в реальном времени.

**Кнопка ON/OFF** — включить/выключить панель

### 3. Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| **L** | Сделать снимок в консоль (детальный лог) |
| Кнопка ON/OFF | Скрыть/показать панель |

---

## 📊 Что показывает панель

### Блок 1: ФИЗИКА (Cannon.js)
```
━━━ ФИЗИКА (Cannon.js) ━━━
Позиция: X=0.00 Y=1.48 Z=0.00
Скорость: 0.00 м/с (0.0 км/ч)
Velocity: X=0.00 Y=0.00 Z=0.00
Rotation (rad): X=0.00 Y=0.00 Z=0.00
Rotation (deg): X=0° Y=0° Z=0°
```
- **Позиция** — где находится физическое тело (Cannon.js)
- **Скорость** — текущая скорость грузовика
- **Velocity** — вектор скорости по осям
- **Rotation** — углы поворота (радианы и градусы)

### Блок 2: ВИЗУАЛ (Three.js)
```
━━━ ВИЗУАЛ (Three.js) ━━━
Позиция: X=0.00 Y=1.48 Z=0.00
Rotation: X=0.00 Y=0.00 Z=0.00
```
- Где находится визуальная модель грузовика

### Блок 3: СИНХРОНИЗАЦИЯ
```
━━━ СИНХРОНИЗАЦИЯ ━━━
ΔPos: X=0.000 Y=0.000 Z=0.000
Статус: ✅ OK
```
- **ΔPos** — разница между физикой и визуалом
- **Статус:**
  - ✅ OK — синхронизация хорошая (разница < 0.1)
  - ❌ РАССИНХРОН — визуал отстаёт от физики!

### Блок 4: УПРАВЛЕНИЕ
```
━━━ УПРАВЛЕНИЕ ━━━
Gas: ✅ | Brake: ⬜
Left: ⬜ | Right: ⬜
Handbrake: ⬜
Steering: 0.00 (target: 0.00)
```
- **Gas/Brake/Left/Right** — какие кнопки нажаты
- **Handbrake** — 🚨 АКТИВЕН если нажат
- **Steering** — текущий угол поворота колёс

### Блок 5: КОЛЕСО #0
```
━━━ КОЛЕСО #0 (переднее левое) ━━━
Rotation: 0.00 rad
SuspensionLength: 0.300 м
Steering: 0.00 rad (0°)
```
- **Rotation** — сколько колесо прокрутилось (качение)
- **SuspensionLength** — текущая длина подвески
- **Steering** — угол поворота колеса

### Блок 6: КАМЕРА
```
━━━ КАМЕРА ━━━
Позиция: X=0.00 Y=8.00 Z=15.00
Смотрит: X=0.00 Y=-0.45 Z=-0.89
```
- Где находится камера и куда смотрит

### Блок 7: КЛАВИШИ
```
━━━ КЛАВИШИ (последние 5) ━━━
12:34:56 | KEY DOWN: w (KeyW)
12:34:57 | KEY UP: w (KeyW)
```
- История последних нажатий клавиш

---

## 🔍 Как диагностировать проблемы

### Проблема 1: Грузовик не едет
**Что смотреть:**
- Блок "УПРАВЛЕНИЕ" — нажимаются ли кнопки?
- Блок "ФИЗИКА" — меняется ли Velocity?
- Блок "КЛАВИШИ" — регистрируются ли нажатия?

**Если Gas ⬜ при нажатии W:**
→ Проблема в TruckController (не регистрирует нажатия)

**Если Gas ✅ но Velocity = 0:**
→ Проблема в Physics (не применяется сила)

### Проблема 2: Грузовик едет не туда
**Что смотреть:**
- Блок "ФИЗИКА" — Rotation (deg)
- Если Y (yaw) = 0° → грузовик смотрит вперёд
- Если Y = 90° → грузовик смотрит вправо
- Если Y = 180° → грузовик смотрит назад

**Проверь:**
- При нажатии W Velocity.Z должна меняться (вперёд/назад по Z)
- При нажатии A/D Rotation.Y должна меняться

### Проблема 3: Рассинхронизация
**Что смотреть:**
- Блок "СИНХРОНИЗАЦИЯ" — ΔPos
- Если ΔPos > 1.0 → визуал отстал от физики!

**Решение:**
→ Проверь метод `syncVisualModel()` в Physics.js

### Проблема 4: Грузовик падает/летает
**Что смотреть:**
- Блок "ФИЗИКА" — Позиция Y
- Если Y < 0 → провалился сквозь землю
- Если Y > 10 → летает в небе

**Решение:**
- Увеличь начальную позицию Y (в Physics.js)
- Проверь создание земли (groundBody)

### Проблема 5: Колёса не поворачиваются
**Что смотреть:**
- Блок "КОЛЕСО #0" — Steering
- При нажатии A/D должно меняться от -0.5 до 0.5

**Если Steering = 0 всегда:**
→ Проблема в applyControls() (не применяется поворот)

### Проблема 6: Неправильная ориентация
**Что смотреть:**
- Блок "ФИЗИКА" — Rotation (deg)
- Сравни с визуалом

**Если физика X=0° Y=0° Z=0° но грузовик повёрнут:**
→ Проблема в начальной ротации Truck.js (bodyGroup.rotation.y)

---

## 💡 Полезные команды в консоли

### Сделать детальный снимок:
```javascript
// Нажми клавишу L или:
game.debugLogger.logToConsole();
```

### Проверить конкретное значение:
```javascript
// Позиция физики
console.log(game.physics.chassisBody.position);

// Позиция визуала
console.log(game.truck.group.position);

// Управление
console.log(game.truckController.controls);

// Колёса
console.log(game.physics.vehicle.wheelInfos);
```

### Телепортировать грузовик:
```javascript
game.physics.chassisBody.position.set(0, 2, 0);
game.physics.chassisBody.velocity.set(0, 0, 0);
```

### Изменить параметры на лету:
```javascript
// Увеличить силу двигателя
game.physics.updateParams({ maxForce: 3000 });

// Больше скольжения
game.physics.updateParams({ frictionSlip: 1.0 });
```

---

## 🎯 Что делать с данными

### 1. Скопируй информацию из панели
- Нажми L чтобы залогировать в консоль
- Скопируй вывод

### 2. Отправь мне:
```
Проблема: [опиши что не так]

DEBUG SNAPSHOT:
━━━ ФИЗИКА ━━━
Позиция: X=... Y=... Z=...
...
```

### 3. Я увижу точно что сломалось:
- Координаты не те?
- Управление не работает?
- Рассинхрон физики и визуала?
- Колёса не поворачиваются?

---

## 🗑️ Как удалить панель

### Временно скрыть:
Нажми кнопку **OFF** в правом углу панели

### Полностью отключить:
В `js/Game.js`, закомментируй строки:
```javascript
// console.log('[Game] 9. Создание отладочной панели...');
// this.debugLogger = new DebugLogger(this);
```

И в методе `update()`:
```javascript
// if (this.debugLogger) {
//     this.debugLogger.update(timestamp);
// }
```

---

## 📝 Пример вывода

Вот как выглядит нормальный вывод (грузовик стоит на месте):

```
━━━ ФИЗИКА (Cannon.js) ━━━
Позиция: X=0.00 Y=1.48 Z=0.00
Скорость: 0.00 м/с (0.0 км/ч)
Velocity: X=0.00 Y=-0.02 Z=0.00
Rotation (rad): X=0.00 Y=0.00 Z=0.00
Rotation (deg): X=0° Y=0° Z=0°

━━━ ВИЗУАЛ (Three.js) ━━━
Позиция: X=0.00 Y=1.48 Z=0.00
Rotation: X=0.00 Y=0.00 Z=0.00

━━━ СИНХРОНИЗАЦИЯ ━━━
ΔPos: X=0.000 Y=0.000 Z=0.000
Статус: ✅ OK

━━━ УПРАВЛЕНИЕ ━━━
Gas: ⬜ | Brake: ⬜
Left: ⬜ | Right: ⬜
Handbrake: ⬜
Steering: 0.00 (target: 0.00)
```

---

**Теперь у тебя есть полный контроль над отладкой! 🚀**

Обнови страницу (F5) и смотри на панель в правом верхнем углу!

