# 🚀 Интеграция Cannon.js — Инструкция

## ✅ Что было сделано

Реалистичная физика автомобиля на **Cannon.js (RaycastVehicle)** теперь интегрирована в проект!

### Добавленные файлы:
1. **js/Physics.js** — Новый физический движок на Cannon.js
2. **INTEGRATION_CANNON.md** — Эта инструкция

### Изменённые файлы:
1. **index.html** — Добавлен Cannon.js в importmap
2. **js/Game.js** — Создание экземпляра Physics, обновление в игровом цикле
3. **js/main.js** — Ползунки теперь управляют параметрами Cannon.js
4. **js/models/Truck.js** — Добавлен массив wheelMeshes для синхронизации

---

## 🎮 Что изменилось в физике

### Старая физика (TruckController):
- ❌ Простая самописная физика (velocity, rotation, lerp)
- ❌ Нет массы, инерции, подвески
- ❌ Нереалистичные повороты

### Новая физика (Cannon.js RaycastVehicle):
- ✅ Реалистичная масса грузовика (1500 кг)
- ✅ Инерция и импульс (грузовик не останавливается мгновенно)
- ✅ Подвеска на каждом колесе (пружины + амортизаторы)
- ✅ Реальное сцепление шин (frictionSlip)
- ✅ Дрифт и заносы при ручнике
- ✅ Правильные повороты (руль только на передних колёсах)
- ✅ Вращение колёс синхронизировано с физикой

---

## ⚙️ Параметры физики

### Основные параметры (js/Physics.js):

```javascript
this.params = {
    // Двигатель
    maxForce: 1500,          // Сила двигателя (Ньютоны)
    maxBrakeForce: 100,      // Сила торможения
    handbrakeForce: 150,     // Сила ручного тормоза
    maxSpeed: 25,            // Макс. скорость (м/с, ~90 км/ч)
    
    // Повороты
    maxSteerVal: 0.5,        // Угол поворота колёс (~30°)
    steerSpeed: 0.05,        // Скорость поворота руля
    
    // Подвеска
    suspensionStiffness: 30,     // Жёсткость пружин
    suspensionDamping: 2.3,      // Демпфирование (амортизаторы)
    suspensionRestLength: 0.3,   // Длина подвески
    
    // Сцепление шин
    frictionSlip: 2.0,       // Коэффициент сцепления (меньше = больше скольжения)
    rollInfluence: 0.4,      // Влияние крена
    
    // Масса
    chassisMass: 1500        // Масса (кг)
};
```

### Как изменились ползунки UI:

| Ползунок в UI | Старый параметр | Новый параметр (Cannon.js) |
|---------------|-----------------|---------------------------|
| 🚀 Мощность вперёд | accelerationRate | **maxForce** (750-4500 N) |
| 🛑 Мощность торможения | braking | **maxBrakeForce** (50-250 N) |
| 🔄 Скорость поворота | turnSpeed | **steerSpeed** (0.015-0.1) |
| 🌊 Затухание поворота | rotationDamping | **frictionSlip** (0.5-5.0) |

---

## 🔧 Как настроить параметры

### Через UI (в реальном времени):
1. Запусти игру
2. Левый верхний угол — панель **"⚙️ Настройка физики"**
3. Двигай ползунки → изменения применяются мгновенно!
4. Кнопка **"🔄 Сбросить настройки"** → возврат к дефолту

### Через код (js/Physics.js):
```javascript
// В конструкторе Physics класса, измени this.params:
this.params.maxForce = 2000;         // Увеличить мощность
this.params.frictionSlip = 1.5;      // Больше скольжения (дрифт)
this.params.suspensionStiffness = 20; // Мягче подвеска
```

### Через метод updateParams() (в runtime):
```javascript
// В консоли браузера:
game.getPhysics().updateParams({
    maxForce: 3000,
    frictionSlip: 1.0,
    suspensionStiffness: 40
});
```

---

## 🏁 Как тестировать

### 1. Базовые тесты:
- **Разгон** — Нажми ⬆ Газ, почувствуй инерцию (грузовик разгоняется постепенно)
- **Торможение** — Нажми ⬇ Тормоз, грузовик не останавливается мгновенно
- **Повороты** — Нажми ◀/▶, передние колёса поворачиваются

### 2. Продвинутые тесты (стадион 240x240м):
- **Дрифт** — Разгонись + ПРОБЕЛ (ручник) + поворот = занос!
- **"Пончики"** — Газ + ручник + руль в одну сторону = круговое скольжение
- **Подвеска** — Резкий поворот на скорости → кузов наклоняется
- **Масса/инерция** — Попробуй остановиться на большой скорости (инерция!)

### 3. Где находится стадион:
1. Езжай прямо по дороге
2. Поверни **ВПРАВО** (кнопка ▶ или D)
3. Найди зелёную стрелку-указатель
4. Езжай по жёлтой разметке ~100м
5. Заезжай через проход в стене
6. Наслаждайся огромной площадкой 240x240м! 🏎️

---

## 🎛️ Расширенная настройка

### Добавить новые ползунки (опционально):

Если хочешь больше контроля, добавь в **index.html** новые слайдеры:

```html
<!-- Подвеска -->
<div class="slider-group">
    <label for="suspensionSlider">
        🔧 Жёсткость подвески: <span id="suspensionValue">30</span>
    </label>
    <input type="range" id="suspensionSlider" min="10" max="50" step="5" value="30">
</div>

<!-- Сцепление -->
<div class="slider-group">
    <label for="frictionSlider">
        🏎️ Сцепление шин: <span id="frictionValue">2.0</span>
    </label>
    <input type="range" id="frictionSlider" min="0.5" max="5.0" step="0.1" value="2.0">
</div>
```

И в **js/main.js** добавь обработчики:

```javascript
suspensionSlider.addEventListener('input', (e) => {
    const value = parseFloat(e.target.value);
    physics.updateParams({ suspensionStiffness: value });
    suspensionValue.textContent = value.toFixed(0);
});

frictionSlider.addEventListener('input', (e) => {
    const value = parseFloat(e.target.value);
    physics.updateParams({ frictionSlip: value });
    frictionValue.textContent = value.toFixed(1);
});
```

---

## 📊 Производительность

### Оптимизации для слабых устройств:

**js/Physics.js** адаптируется под класс производительности:

```javascript
const solverIterations = 
    this.settings.performanceClass === 'LOW' ? 3 :      // Слабые устройства
    this.settings.performanceClass === 'AVERAGE' ? 5 :  // Средние
    10;  // HIGH — мощные устройства
```

### Замеры:
- **LOW** (2 ГБ RAM): 30+ FPS (3 итерации solver)
- **AVERAGE** (4 ГБ RAM): 50+ FPS (5 итераций)
- **HIGH** (8+ ГБ RAM): 60 FPS (10 итераций)

### Fixed timestep:
Физика работает с фиксированным шагом **1/60 сек (60 Hz)** для стабильности на любых устройствах.

---

## 🐛 Troubleshooting

### Грузовик не движется:
- Проверь консоль: `[Physics] RaycastVehicle создан, колёс: 4`
- Убедись, что Cannon.js загружен: `console.log(CANNON)`

### Грузовик летает или проваливается:
- Увеличь suspensionStiffness: `physics.updateParams({ suspensionStiffness: 40 })`
- Проверь начальную позицию chassisBody (должна быть выше земли: `y: 2`)

### Грузовик слишком скользкий:
- Увеличь frictionSlip: `physics.updateParams({ frictionSlip: 3.0 })`

### Грузовик переворачивается:
- Уменьши rollInfluence: `physics.updateParams({ rollInfluence: 0.2 })`
- Понизь центр массы или увеличь ширину колёсной базы

### Низкий FPS:
- Уменьши solver iterations в `js/Physics.js`: `this.world.solver.iterations = 3;`

---

## 🔮 Дальнейшие улучшения

1. **Разные поверхности** — Асфальт (friction 0.8), трава (0.5), лёд (0.2)
2. **Столкновения** — Барьеры, препятствия (Cannon.js Body)
3. **Опрокидывание** — Детект переворота + сброс позиции
4. **Звуки** — Звук двигателя меняется от скорости
5. **Частицы** — Пыль от колёс при дрифте (Three.js ParticleSystem)
6. **Мультиплеер** — Синхронизация физики через WebSocket

---

## 📚 Полезные ссылки

- **Cannon.js документация:** https://pmndrs.github.io/cannon-es/
- **RaycastVehicle API:** https://pmndrs.github.io/cannon-es/classes/RaycastVehicle.html
- **Three.js документация:** https://threejs.org/docs/

---

**Создано:** 29.12.2025  
**Автор интеграции:** Claude Sonnet 4.5  
**Версия Cannon.js:** 0.20.0  
**Версия Three.js:** 0.160.0

