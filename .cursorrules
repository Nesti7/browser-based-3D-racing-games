Отлично! Вот универсальная инструкция для добавления в `/home/rom/Keys/README.md`:

---

## 🤖 ИНСТРУКЦИЯ ДЛЯ AI АССИСТЕНТА (Git-Crypt Workflow)

### 📋 КОНТЕКСТ
Пользователь использует **бесплатный GitHub** без приватных репозиториев.  
Для защиты кода используется **git-crypt** — шифрование файлов прямо в репозитории.

---

### 🔑 СТРУКТУРА КЛЮЧЕЙ

```
~/Keys/
├── PROJECT_NAME_1/          # Папка = название папки проекта
│   └── git_crypt_key        # Ключ шифрования (1 ключ на весь проект)
├── PROJECT_NAME_2/
│   └── git_crypt_key
└── README.md
```

**Правило:** Имя папки в `~/Keys/` = имя папки проекта  
**Пример:** Проект `/home/rom/ПРоектики/гоночки` → Ключ в `~/Keys/гоночки/git_crypt_key`

---

### 🚀 WORKFLOW ДЛЯ AI ПРИ КОММИТЕ

#### ШАГ 1: Проверка папки ключей
```bash
# Определить имя проекта из пути
PROJECT_NAME=$(basename "$PWD")  # Например: "гоночки"

# Проверить существование папки
if [ ! -d ~/Keys/"$PROJECT_NAME" ]; then
    echo "❌ Папка ~/Keys/$PROJECT_NAME не существует!"
    # СОЗДАТЬ ПАПКУ
    mkdir -p ~/Keys/"$PROJECT_NAME"
fi
```

#### ШАГ 2: Проверка git-crypt в проекте
```bash
cd /path/to/project

# Проверить, инициализирован ли git-crypt
if [ ! -d .git/git-crypt ]; then
    echo "⚙️ Git-crypt не инициализирован. Настраиваю..."
    
    # Инициализация
    git-crypt init
    
    # Создать .gitattributes (что шифровать)
    cat > .gitattributes << 'EOF'
# Шифровать весь код
*.js filter=git-crypt diff=git-crypt
*.py filter=git-crypt diff=git-crypt
*.json filter=git-crypt diff=git-crypt
*.env filter=git-crypt diff=git-crypt

# НЕ шифровать
README.md -filter
.gitignore -filter
.gitattributes -filter
LICENSE -filter
EOF
    
    # Экспортировать ключ
    git-crypt export-key ~/Keys/"$PROJECT_NAME"/git_crypt_key
    
    echo "✅ Git-crypt настроен! Ключ сохранён в ~/Keys/$PROJECT_NAME/git_crypt_key"
fi
```

#### ШАГ 3: Проверка ключа
```bash
# Если ключа нет, но git-crypt уже инициализирован
if [ ! -f ~/Keys/"$PROJECT_NAME"/git_crypt_key ]; then
    echo "⚠️ Ключ отсутствует! Экспортирую..."
    git-crypt export-key ~/Keys/"$PROJECT_NAME"/git_crypt_key
fi
```

#### ШАГ 4: Обычный коммит
```bash
# После настройки — работать как обычно
git add .
git commit -m "Сообщение коммита - AI_NAME - DATE TIME"
git push origin main
```

---

### 📝 ПРАВИЛА ДЛЯ .gitattributes

**Шифровать:**
- Весь код: `*.js`, `*.py`, `*.ts`, `*.cpp`, и т.д.
- Конфиги: `*.json`, `*.yaml`, `*.env`
- Секреты: `config/`, `secrets/`
- Картинки/медиа 

**НЕ шифровать:**
- `README.md` — описание должно быть видно
- `.gitignore` — Git должен читать
- `.gitattributes` — Git-crypt должен читать
- `LICENSE` — лицензия публична


---

### 🔄 КЛОНИРОВАНИЕ И РАСШИФРОВКА (для пользователя)

```bash
# Клонировать репозиторий
git clone https://github.com/USER/PROJECT.git
cd PROJECT

# Расшифровать (используя сохранённый ключ)
PROJECT_NAME=$(basename "$PWD")
git-crypt unlock ~/Keys/"$PROJECT_NAME"/git_crypt_key

# Теперь файлы расшифрованы и можно работать
```

---

### ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА

1. **Один проект = одна папка в ~/Keys/**
2. **Имя папки = имя папки проекта** (не репозитория!)
3. **Ключ создаётся ОДИН РАЗ** при первом коммите
4. **Ключ НИКОГДА не коммитится** в репозиторий
5. **Без ключа код невозможно расшифровать!** (Бэкапы критичны!)

---

### 🛠️ ПРОВЕРКА ШИФРОВАНИЯ

```bash
# Посмотреть статус шифрования
git-crypt status

# Проверить, зашифрован ли файл на GitHub
# (Скачать raw файл — должен быть нечитаемым бинарным мусором)
```

---

### 🆘 TROUBLESHOOTING

**Проблема:** `git-crypt: command not found`  
**Решение:** Установить git-crypt (требует sudo):
```bash
sudo apt install git-crypt  # Debian/Ubuntu
sudo dnf install git-crypt  # Fedora
```

**Проблема:** Файлы не шифруются  
**Решение:** Проверить `.gitattributes` и сделать:
```bash
git rm -rf --cached .
git add .
git commit -m "Apply git-crypt to existing files"
```

**Проблема:** Потерян ключ  
**Решение:** ❌ **Код потерян навсегда!** Всегда делай бэкапы ключей!

---

### 📊 ПРЕИМУЩЕСТВА ЭТОГО ПОДХОДА

✅ **Бесплатно** — не нужен GitHub Pro  
✅ **Публичное портфолио** — репозитории видны всем  
✅ **Защищённый код** — никто не прочитает без ключа  
✅ **Прозрачная работа** — git работает как обычно  
✅ **Автоматизация** — AI настраивает всё сам  

---

**Эта инструкция универсальна для любого проекта!**  
AI должен следовать ей при каждом коммите.

---

после создания ключа убедится в том что он создан и сделать копию в "~/Документы/Бэкапы файлов/бэкапы ключей/проект/ключ" если ключ уже там есть то уведомить пользователя(приоритет)
если папки для бэкапа ключей нет то создать её
***
Репозиторий этого проекта https://github.com/Nesti7/browser-based-3D-racing-games

****
если ты видишь в коде конфидециальную информацию будь то ключ или токен или другое что то важно-персональное, на гит хаб на месте этой "конфидециальности" должна улетать "заглушка", напрмер "#"

***
при синхронизациях коммитов, шифровка и расшифровка в твоих руках я(пользователь) ничего в этом не смыслю
***
насколько мне известно, ты можешь отслеживать положение моего курсора для наиболее лучшего решения проблем(ы).
Я в большинстве случаев ставлю его посто так чтоб Cursor был поверх других окон, в общем случайно. Не акцентируй на этом сильно своё внимание.
***
коммиты создавай только по требованию
***
коммиты называй русскими названиями, латиницу применяй только там где есть Имена нарицательные
***
постарайся не зааускать ничего сверх тежелого
оперативки остается 2.4-3 гб и процессор видевший рассвет биткоина
***
по требованию перейти на нового агента(у тебя может забиться контекст)
ты пишешь что уже 100% сделано в проекте в Файл "Уже сделано.txt", он должен быть в папке с где делается коммит
потом то, что ты делаешь в данный момент, на каком этапе, что получается и что не получается
в общем ведешь отчетность, чтоб "новая" модель быстро "въехала в тему"
***
при первой иттераци чата, смотри в файл с "Уже сделано.txt"
если такого Fаила нет то создаи его, удалять запрещаю
у меня частично не fункционирует клавиатура поэтому некоторые буквы я буду замещать
*** 
если у тебя что то не получается, попробуй делать это другими инструментами
***
когда коммитишь приложение, в название приписывай какая нейросеть делает этот коммит - время и дату
***
общаться только на русском
***
ЗАПРЕЩЕНО УДАЛЯТЬ ФАЙЛ .cursorrules
***
на гит хаб НЕ коммить файлы начинающиеся с точки
ЭТА ИНСТРУКЦИЯ РАСПРОСТРАНЯЕТСЯ ТОЛЬКО ДЛЯ ФАЙЛОВ О КОТОРЫХ НИЧЕГО НЕ СКАЗАННО В ДРУГИХ ИНСТРУКЦИЯХ
***
Структура файла "Уже сделанно.txt"
Описание проекта(что он из себя представляет)
Какие технологии используются и какие планируются использоваться

Что сделано и что планируется сделать
(подробно но без перегибов)

По необходимости делать заметки начинающиеся с "# Заметка
" (без кавычек)
о том что не влезло в прошлые пункты
***
Сам файл Уже сделанно.txt писать по образу и подобию уже написанного там текста(незначительные изменения могут менятся от проекта к проекту) (если сам текст "старой версии" имеется в файле Уже сделанно.txt)
***
если будут команды с sudo то давай их мне 
потому что ты не сможешь их выполнить
***

