# 🚚 3D Гонки на Грузовиках

Telegram Mini App с 3D-симулятором вождения грузовиков в мультяшном low-poly стиле.

## 🎮 Особенности

- **Low-poly графика** в стиле toon shading (мультяшный)
- **Адаптивная производительность** под LOW/AVERAGE/HIGH устройства
- **Touch-управление** (кнопки: Газ, Тормоз, Влево, Вправо)
- **Процедурная генерация** моделей (готово к замене на .glb)
- **Оптимизация для слабых Android-устройств** (2-4 ГБ RAM)

## 🚀 Быстрый старт

### Локальный запуск

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/Nesti7/browser-based-3D-racing-games.git
cd browser-based-3D-racing-games
```

2. **Запустите локальный сервер:**
```bash
python3 -m http.server 8000
```

3. **Откройте в браузере:**
```
http://localhost:8000
```

### Запуск в Telegram

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Настройте Mini App через `/newapp`
3. Укажите URL вашего сервера (ngrok/localhost tunnel)
4. Откройте приложение в Telegram

## 🎯 Управление

### Движение:

**На мобильных устройствах:**
- **⬆ Газ** — ускорение
- **⬇ Тормоз** — торможение/задний ход
- **◀ Влево** — поворот влево
- **▶ Вправо** — поворот вправо

**На ПК (для тестирования):**
- **W / ↑** — газ
- **S / ↓** — тормоз
- **A / ←** — влево
- **D / →** — вправо

### Камера:

**На ПК:**
- **Правая кнопка мыши (ПКМ)** + движение — вращение камеры вокруг грузовика
- **Колесо мыши** — приближение/отдаление

**На мобильных:**
- **Два пальца** — вращение камеры

## 📁 Структура проекта

```
/
├── index.html              # Главная страница
├── style.css               # Стили и UI
├── js/
│   ├── main.js            # Точка входа
│   ├── Game.js            # Игровой класс
│   ├── TruckController.js # Управление грузовиком
│   ├── PerformanceManager.js # Адаптация графики
│   └── models/
│       ├── Truck.js       # Модель грузовика
│       └── Environment.js # Окружение (дорога, деревья)
├── README.md              # Документация
└── Уже сделано.txt        # Отчётность по проекту
```

## 🔧 Технологии

- **Three.js r160+** — 3D-движок (WebGL)
- **Telegram WebApp API** — интеграция с Telegram
- **Vanilla JavaScript** (ES6 модули)
- **Без сборщиков** — работает сразу в браузере

## ⚙️ Адаптация производительности

### LOW (слабые устройства):
- Pixel ratio: 0.75
- FPS: 30
- Тени: отключены
- Деревья: до 20 шт.
- Туман: 20-50 м

### AVERAGE:
- Pixel ratio: 1.0
- FPS: 60
- Тени: включены
- Деревья: до 40 шт.
- Туман: 40-100 м

### HIGH:
- Pixel ratio: до 2.0
- FPS: 60
- Тени: включены
- Деревья: до 60 шт.
- Туман: 60-150 м

## 🛠️ Разработка

### Замена процедурных моделей на готовые .glb:

1. Добавьте `.glb` файл в проект
2. Импортируйте `GLTFLoader` из Three.js:
```javascript
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
```
3. Загрузите модель и используйте метод `replaceWithGLTF()`:
```javascript
const loader = new GLTFLoader();
loader.load('models/truck.glb', (gltf) => {
    truck.replaceWithGLTF(gltf.scene);
});
```

### Добавление новых функций:

- **Физика** — можно интегрировать Cannon.js
- **Звуки** — Web Audio API
- **Мультиплеер** — WebSocket + серверная часть
- **UI** — добавить спидометр, таймер, счёт

## 🐛 Отладка

- **FPS и Performance Class** отображаются в левом верхнем углу
- **Консоль браузера** — подробные логи
- **Telegram Desktop** — для тестирования Mini App локально

## 📝 Лицензия

MIT License

## 👤 Автор

Проект создан для Telegram Mini Apps Contest

